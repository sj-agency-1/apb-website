---
import Badge from '@components/ui/Badge.astro';
import Buttons from '@components/ui/Buttons.astro';
import Heading from '@components/ui/Heading.astro';
import Image from '@components/ui/Image.astro';
import Paragraph from '@components/ui/Paragraph.astro';
import Proof from '@components/ui/Proof.astro';
import Tagline from '@components/ui/Tagline.astro';
import type { BlockSchema } from '@components/schemas/block';
import Container from '@components/structures/Container.astro';
import Group from '@components/structures/Group.astro';
import Prose from '@components/structures/Prose.astro';
import Section from '@components/structures/Section.astro';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
interface Props extends BlockSchema {}

const { depth = 2, badge, tagline, heading, paragraph, content, buttons, image, proof, ...rest } = Astro.props;
---

<Section class:list="hero-1" {...rest}>
  <Container class="flex flex-col items-center gap-16">
    <Prose align="center">
      <Proof {...proof} />
      <Badge size="lg" {...badge} />
      <Tagline size="sm" text={tagline} />
      <Heading size="6xl" text={heading} {depth} />
      <Paragraph class="text-balance" size="xl" text={paragraph} />
      <Fragment set:html={content} />
      <Group>
        <Buttons size="lg" {buttons} />
      </Group>
    </Prose>
    <Image class="mx-auto max-h-[700px] w-auto rounded-lg" {...image} />
  </Container>
</Section>
